=== תהליך פיתוח השרת והלמידה בעזרת AI ===

שלב 1: תכנון מבנה הפרויקט וחלוקה לקבצים
- פנייה ל-AI לקבלת הסבר על חלוקת הפרויקט הבסיסי למודולים.
- החלטה על יצירת שלושה קבצים: app.js (קובץ ראשי), courses.js (ניהול קורסים), ו-sudents.js (ניהול תלמידים) על מנת לשמור על סדר והפרדת סמכויות בקוד.

שלב 2: יצירת המודולים וייצוא הנתונים
- הגדרת מערכי הנתונים בקבצים הנפרדים.
- שימוש בפקודה module.exports בכל קובץ כדי לאפשר לקובץ הראשי לגשת לנתונים.

שלב 3: הקמת שרת Express והגדרת ה-Routes
- כתיבת הקוד הראשי ב-app.js.
- ייבוא המודולים בעזרת require.
- הגדרת שלושת נתיבי ה-GET הנדרשים (/, courses/, students/) להחזרת מידע בפורמט JSON בלבד, והאזנה לפורט 3000.

שלב 4: זיהוי ופתרון שגיאת MODULE_NOT_FOUND
- בעת הרצת השרת בטרמינל התקבלה שגיאת MODULE_NOT_FOUND בשורה 6 בקובץ app.js.
- פנייה ל-AI לקבלת עזרה והבנה שהשגיאה נובעת מחוסר התאמה בין שם הקובץ הפיזי בתיקייה (sudents.js) לבין השם שנכתב בפקודת ה-require.
- התיקון: עדכון נתיב הייבוא בתוך קובץ app.js שיתאים בדיוק לשם הקובץ בתיקייה, ושמירת הקובץ מחדש.

שלב 5: פתרון בעיית מערך הנתונים הריק ואתחול השרת
- לאחר הרצת השרת, הדפדפן הציג דף ריק בנתיב של התלמידים.
- בעזרת ה-AI בוצעה בדיקה של קובץ sudents.js ונמצא כי היה חסר ייצוא של המערך באמצעות module.exports. 
- בנוסף, בוצע ריענון לשרת בטרמינל על ידי עצירתו עם Ctrl+C והפעלתו מחדש (node app.js) כדי שהשינויים ייכנסו לתוקף. לאחר מכן המידע הוצג בהצלחה בדפדפן.

שלב 6: שיפור הקוד ושאילת שאלות להרחבה
בשלב זה בוצע תהליך חשיבה מול ה-AI במטרה להבין טוב יותר את הקוד ולשפרו:
1. אילו שגיאות עשויות להיות בקוד?
   - שגיאת פורט תפוס (EADDRINUSE) במידה ותוכנה אחרת כבר רצה על פורט 3000.
   - שגיאות תחביר (Syntax Errors) או חוסר התקנה של ספריית express (ניתן לפתרון ע"י npm install).
2. איך אפשר לשפר את הקוד?
   - הוספת מנגנון טיפול בשגיאות (Error Handling Middleware) כדי להחזיר תגובת JSON מסודרת (כמו שגיאה 404) במידה ומשתמש מנסה לגשת לנתיב שאינו קיים.
   - שימוש במשתני סביבה (קובץ .env) כדי לנהל את מספר הפורט בצורה דינמית ומאובטחת יותר.
3. הערות על מבנה הקוד:
   - חלוקת הקוד לקבצים נפרדים (הפרדת סמכויות) מבוצעת בצורה נכונה ומקצועית. בפרויקטים גדולים יותר, מומלץ להעביר גם את הגדרות ה-Routes עצמן לקבצים נפרדים באמצעות express.Router().
   --שיעור 3
   text_content = """תיעוד שאלות ותשובות לבדיקת שרת ה-Express (CRUD)

שורה חובה להוספה בשרת:
שאלה: מהי השורה שחובה להוסיף לפני הגדרת ה-routes כדי שהשרת יתמוך בבקשות JSON?
תשובה: app.use(express.json());

ישות: קורסים (Courses)
1. GET (רשימה)
   שאלה: כיצד לבקש את רשימת כל הקורסים הקיימים במערכת?
   תשובה: שליחת בקשת GET לכתובת http://localhost:3000/courses

2. GET (בודד)
   שאלה: כיצד לקבל פרטים של קורס ספציפי בעל מזהה (id) שערכו 1?
   תשובה: שליחת בקשת GET לכתובת http://localhost:3000/courses/1

3. POST (יצירה)
   שאלה: כיצד להוסיף קורס חדש למערכת, ואיזה מבנה נתונים יש לשלוח ב-Body?
   תשובה: שליחת בקשת POST לכתובת http://localhost:3000/courses. ב-Body (סוג raw JSON) יש לשלוח אובייקט עם השדות name ו-duration.

4. PUT (עדכון)
   שאלה: כיצד לעדכן שם או משך זמן של קורס קיים שמזההו 1?
   תשובה: שליחת בקשת PUT לכתובת http://localhost:3000/courses/1 ושליחת הנתונים המעודכנים ב-Body בפורמט JSON.

5. DELETE (מחיקה)
   שאלה: כיצד למחוק לחלוטין קורס מהמערכת לפי המזהה שלו?
   תשובה: שליחת בקשת DELETE לכתובת http://localhost:3000/courses/1 (אין צורך בגוף בקשה).

ישות: תלמידים (Students)
שאלה: מהם הנתיבים הבסיסיים לביצוע פעולות CRUD על ישות התלמידים?
תשובה: הכתובת המרכזית היא http://localhost:3000/students, כאשר עבור קבלת תלמיד בודד, עדכון או מחיקה מוסיפים את המזהה בסוף הכתובת (לדוגמה: /students/1).

ישות: רישום (Enrollments)
שאלה: מהי מטרת בקשת ה-POST בישות הרישום ואילו שדות נדרשים ב-Body?
תשובה: המטרה היא לקשר תלמיד לקורס מסוים. ב-Body יש לשלוח אובייקט JSON המכיל את השדות studentId ו-courseId (ושדה date אופציונלי). הכתובת היא http://localhost:3000/enrollments.
"""

with open("lesson_questions_documentation.txt", "w", encoding="utf-8") as f:
    f.write(text_content)
    --שיעור 4
    ==================================================
פרויקט ניהול מכללה - תיעוד תהליך הפיתוח והארכיטקטורה
==================================================

קובץ זה מתעד את השאלות והשלבים שבוצעו מול ה-AI לצורך סידור מחדש (Refactoring) של הפרויקט:

שאלות ותשובות במהלך העבודה:
--------------------------------
1. שאלה: מהם היתרונות של חלוקת פרויקט Node.js למספר קבצים?
   - תשובה: שיפור הקריאות והתחזוקה של הקוד, אפשרות לשימוש חוזר בפונקציות (Reusability), הפרדת סמכויות ברורה בין רכיבים, ומניעת התנגשויות בעבודה בצוות.

2. שאלה: מה המשמעות של העיקרון "אחריות אחת לכל קובץ" (Single Responsibility)?
   - תשובה: כל קובץ במערכת צריך לבצע תפקיד מוגדר אחד בלבד. לדוגמה, קובץ נתונים לא מכיר את השרת, וקובץ השרת לא מבצע חישובים לוגיים בעצמו.

3. שאלה: כיצד לבצע את חלוקת התיקיות הנדרשת בתרגיל?
   - תשובה: הפרויקט חולק ל-4 שכבות מרכזיות:
     * data - החזקת מערכי הנתונים הגולמיים (courses, students, enrollments).
     * services - שכבת הלוגיקה העסקית והפונקציות שמנהלות את המידע.
     * controllers - שכבת הקישור שמקבלת בקשות HTTP ומחזירה תשובות עם סטטוסים מתאימים.
     * routes - שכבת הניתוב שמגדירה את נקודות הקצה (Endpoints) עבור כל ישות.

4. שאלה: כיצד לנהל את השינויים ב-Git לפי דרישות המרצה?
   - תשובה: הקוד הישן עודכן ישירות בתוך פרויקט המקור, ולאחר מכן בוצע קומיט מסודר (Refactor) שמחליף את המבנה המקורי במבנה התיקיות החדש מבלי ליצור פרויקטים מקבילים.

בדיקת תקינות המערכת:
-----------------------
- השרת הורץ בהצלחה על פורט 3000 באמצעות קובץ הרצה מרכזי (index.js).
- בוצעו בדיקות מקיפות בתוכנת Postman עבור כל הישויות (סטודנטים, קורסים והרשמות), וכל נתיבי ה-GET וה-POST מחזירים מידע מדויק ותקין.